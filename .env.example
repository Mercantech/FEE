# PostgreSQL (lokal udvikling — matcher docker-compose db-service)
DATABASE_URL="postgresql://fee:fee@localhost:5432/fee"

# Auth.js — generér fx med: openssl rand -base64 32
AUTH_SECRET=""

# Offentlig base-URL for appen (skal matche Mercantec redirect_uri-origin)
AUTH_URL="http://localhost:3000"

# Mercantec OAuth-klient (registrér i Mercantec Auth — se README)
MERCANTEC_CLIENT_ID=""
# Valgfri — kun for fortrolig klient; ellers tom for ren PKCE
MERCANTEC_CLIENT_SECRET=""
